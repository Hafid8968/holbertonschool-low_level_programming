#include <stdio.h>

// This main function serves as the entry point of the program
int main(void) {
    // Using the puts function to display the desired message with included quotes
    puts("\"Programming is like building a multilingual puzzle\"");

    // The program concludes by returning the value 0
    return 0;
}

